"""Ontology Quality Intelligence (OQI) -- deterministic quality foundation
(CDD-039; OQI1 Artifact Authorization). Capability-scoped, not gate-scoped
(mirrors `app.domain.integration`): this package is the enduring OQI domain
model, not a single-gate package like `app.domain.gate_s`/`app.domain.gate_v`."""
